<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
   <div class="container-fluid">
      <a class="navbar-brand" href="/admin">
      <img src="/images/favicon-admin.png" alt="Admin Panel" width="30" height="30" class="d-inline-block align-text-top">
      Admin Panel
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
         <ul class="navbar-nav ms-auto">
            <li class="nav-item <%= pathname === '/admin' ? 'active' : '' %>">
               <a class="nav-link" href="/admin">Dashboard</a>
            </li>
            <li class="nav-item <%= pathname === '/admin/gameworld' ? 'active' : '' %>">
               <a class="nav-link" href="/admin/gameworld">Game World Data</a>
            </li>
            <li class="nav-item <%= pathname === '/admin/system-stats' ? 'active' : '' %>">
               <a class="nav-link" href="/admin/system-stats">System Stats</a>
            </li>
            <!-- Показываем поле поиска только на странице /admin -->
            <% if (pathname === '/admin') { %>
            <li class="nav-item">
               <input type="text" id="search-input" class="form-control me-2" placeholder="Search by users..." style="width: 250px;">
            </li>
            <% } %>
            <!-- Показываем поле выбора только на странице /admin -->
            <% if (pathname === '/admin') { %>
            <li class="nav-item">
               <select id="users-per-page" class="form-select w-auto d-inline-block" style="width: auto;">
                  <option value="5">5</option>
                  <option value="10" selected>10</option>
                  <option value="25">25</option>
                  <option value="50">50</option>
               </select>
            </li>
            <% } %>
            <!-- Новая ссылка "Visit Website" -->
            <li class="nav-item">
               <a class="nav-link text-info" href="/" target="_blank">Visit Website</a>
            </li>
            <li class="nav-item">
               <a class="nav-link text-danger" href="/admin/logout">Logout</a>
            </li>
         </ul>
      </div>
   </div>
</nav>